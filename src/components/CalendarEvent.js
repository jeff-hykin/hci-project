class CalendarEvent {
    constructor({ date, time, repeatPattern }) {
        this.date = new Date(date)
        this.time = time
        this.repeatPattern = repeatPattern
    }
    dateTime() {
        return 
    }
}

export default CalendarEvent